# 更新记录与版本控制

> 本文件记录项目每次代码更新的主要内容与时间，便于追溯变更历史。

---

## v4.5.1 — 2026-07-03

- 移除 v4.5 金额脱敏功能（`_desensitize_amount` 方法及相关逻辑）
- `_open_file` 方法跨平台兼容：Windows 用 `os.startfile`、macOS 用 `open`、Linux 用 `xdg-open`
- 新增 `GPT图片生成器_mac.spec` macOS 打包配置文件（datas 路径动态获取 customtkinter 安装位置）
- 新增 `.github/workflows/build-mac.yml` GitHub Actions 工作流，推送代码后自动在 macOS 云端打包 `.app`

## v4.5 — 2026-06-25

- 单次/批量出图调用 API 前自动检测提示词中的金额数字并脱敏为 `xxx`，保留金额单位（万/亿/元/块等）
- 三段正则覆盖：货币符号+数字（$5000）、数字+量纲单位（3.5亿元/1200元）、数字+裸单位（5000万）
- 脱敏在 `_generate_one` 入口执行，单次和批量链路同时生效，日志中输出脱敏提示
- 非金额数字（图片张数、尺寸像素、版本号、百分比等）不受影响

## v4.4 — 2026-06-05

- 流式输出改为 `iter_content` 逐块读取 + 手动 SSE 事件分割，修复代理层缓冲导致前5秒无输出的问题
- 文本回复区使用 `insert("end", delta)` 追加模式，实现逐字显示
- 扩展推理模型检测关键词（增加 deep-think / think）
- 兼容多种 reasoning 字段名：`reasoning_content` / `reasoning` / `think` / `thought`
- UI 刷新节流优化：滚动 100ms、update_idletasks 50ms、adjust_height 每 200 字符
- SSE 请求添加 `Accept: text/event-stream` 头

## v4.3.1 — 2026-06-05

- 修复 APP_VERSION 仍显示 v4.2 的问题，同步更新至 v4.3.1
- 配置页 API 密钥标题行右侧添加「API密钥申请」超链接按钮，链接至注册页面
- 按钮旁添加 ⚠️ 图标，悬浮显示使用申明提示文案
- tooltip wraplength 加宽至 360px，确保申明文字完整显示

## v4.3 — 2026-06-04

- 智能分析页支持读取 .docx / .xlsx / .pptx / .pdf 四种文档格式
- Chat 回复区由 Label 改为 CTkTextbox，支持自适应高度与段落保留

## v4.2 — 2026-06-04

- PyInstaller spec excludes 瘦身打包，体积从 42MB 降至 23MB
- 添加 python-docx hiddenimports 依赖
- Textbox 自适应高度优化

## v4.0 — 2026-06-03

- 放弃速创异步协议，全面改用 OpenAI 兼容协议（以云雾 API 为例）
- 新增模型管理：查询/手动添加/保存多个模型（上限10个），下拉框选择
- 新增画质档位：auto / low / medium / high
- 图生图改为 multipart/form-data 文件直传，最多支持 16 张参考图
- 批量出图并发上限提升至 30
- 新增智能分析 Tab（思考过程展示 + 文件分析）
- 新增参考图框缩放优化、模型模糊搜索

## v3.3 — 2026-06-02

- 纯速创单协议稳定版（初始提交）
- 支持单次/批量出图、基础配置

---

## 版本管理约定

| 约定项 | 规则 |
|--------|------|
| 分支策略 | `main` 分支保持可运行的稳定版本 |
| 提交格式 | 中文，`类型: 简要描述`（feat / fix / refactor / style / docs / chore） |
| 版本号规则 | 主版本.次版本.修订号（如 v4.3.1），功能迭代升次版本，补丁升修订号 |
| 代码变更流程 | 修改 → 冒烟测试 → git add/commit/push → 重新打包 EXE |
| GitHub 推送 | 需开启 Clash 代理（127.0.0.1:7897） |
| 打包命令 | `py3.13 -m PyInstaller --clean --noconfirm "GPT图片生成器.spec"` |
